'use strict';

angular.module('mean.signin')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'signin'
      };
    }
  ]);
