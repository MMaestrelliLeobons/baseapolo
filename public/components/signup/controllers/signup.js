'use strict';

angular.module('mean.signup')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'signup'
      };
    }
  ]);
