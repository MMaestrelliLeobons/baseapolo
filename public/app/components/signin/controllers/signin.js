'use strict';

angular.module('mean_signin')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'signin'
      };
    }
  ]);
