'use strict';

angular.module('baseapolo.home.public')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'home.public'
      };
    }
  ]);
