'use strict';

angular.module('baseapolo.signup')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'signup'
      };
    }
  ]);
