'use strict';

angular.module('baseapolo.navbar.public')
	.controller('', ['', 'Global',
	  function(, Global) {
// Original scaffolded code.
      .global = Global;
      .package = {
        name: 'navbar.public'
      };
    }
  ]);
